import { CardAnimationDirective } from './card-animation.directive';

describe('CardAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new CardAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
